services:
  redis:
    image: redis:alpine
    container_name: grocery_dev_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - grocery-net

volumes:
  redis_data:

networks:
  grocery-net: # Custom network name
    driver: bridge